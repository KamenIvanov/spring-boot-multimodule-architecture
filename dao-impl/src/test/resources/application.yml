spring:
  datasource:
    url: jdbc:hsqldb:mem:testmemdb;ifexists=false;sql.syntax_mys=true
    driver-class-name: org.hsqldb.jdbc.JDBCDriver
    username: sa
    password: "test"
  jpa:
    database-platform: org.hibernate.dialect.HSQLDialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    format_sql: true
